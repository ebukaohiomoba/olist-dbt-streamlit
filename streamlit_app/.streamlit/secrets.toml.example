# Copy to secrets.toml and fill in. Never commit secrets.toml.
# When [postgres] is present the app queries Postgres; otherwise it falls back
# to parquet files in data/marts/ (used in cloud deployments).

[postgres]
host = "localhost"
port = 5432
database = "target_ecommerce_raw"
schema = "analytics"
user = "chukwuebukaohiomoba"
password = "1234"
